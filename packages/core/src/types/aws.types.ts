export interface IS3 {
    region: string;
    credentials: {
        accessKeyId: string;
        secretAccessKey: string;
    };
}

export interface IKMS {
    region: string;
    credentials: {
        accessKeyId: string;
        secretAccessKey: string;
    };
}
